[workspace]
members = [
    "serde-name",
    "serde-reflection",
    "serde-generate",
    "serde-generate-bin",
]
resolver = "2"

[profile.release]
debug = true
lto = 'thin'

[profile.bench]
debug = true
